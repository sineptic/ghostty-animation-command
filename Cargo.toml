[package]
name = "ghostty_animation"
version = "0.1.0"
edition = "2021"

[dependencies]
alternate_screen_wrapper = { version = "0.2.1", features = ["crossterm"] }
crossterm = { version = "0.29.0", default-features = false }

[profile.release]
lto = true
codegen-units = 1
